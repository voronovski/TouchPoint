# Touch Point: как устроен продукт и как им пользоваться

> Актуально для текущей реализации на 3 сентября 2026 года. Интерфейс прототипа пока англоязычный, поэтому названия экранов и кнопок ниже приведены так, как они выглядят в приложении.

## Статус и правило обновления документа

Это живое описание реализованного продукта. Оно объясняет не только последовательность нажатий, но и замысел экранов, значение состояний, внутренние правила и границы обещаний Touch Point.

При любом изменении экрана, пользовательского сценария, бизнес-правила, модели данных, хранения, системной интеграции или ограничения этот файл нужно обновлять в той же задаче. Документ должен описывать уже работающую реализацию, а не желаемую возможность. Нереализованные решения относятся в раздел «Открытые вопросы».

## 1. Что такое Touch Point

Touch Point — локальный планировщик важных касаний с людьми. Его задача — помочь один раз занести людей и значимые даты, заранее создать календарь поздравлений, подготовить подходящие тексты и в нужный день привести пользователя к следующему понятному действию.

Главная идея продукта: **планирование важнее оформления**. Touch Point — не каталог открыток и не сервис массовых рассылок. В центре интерфейса находятся человек, повод, дата, способ связи, часовой пояс и готовность действия.

Приложение придерживается нескольких принципов:

- пользователь планирует отношения, а не управляет «лидами» или безличной CRM-базой;
- рабочие и личные отношения живут в одной модели и одном интерфейсе;
- сообщение всегда можно проверить и изменить перед отправкой;
- SMS и email отправляет сам пользователь через системные приложения;
- Apple Intelligence — необязательное ускорение, а не условие работы продукта;
- текущая версия работает локально на одном устройстве, без аккаунта, облачной синхронизации и командных ролей;
- интерфейс напоминает о важном без формулировок, вызывающих чувство вины.

## 2. Короткий путь от первого запуска до отправки

1. На первом запуске выбрать `Work`, `Personal` или `All` и нажать `Continue`.
2. Во вкладке `People` добавить людей, их контакты, предпочтения и ежегодные важные даты.
3. На `Home` нажать `Plan greetings`.
4. Выбрать людей, поводы, способ связи и шаблоны, проверить итог и нажать `Schedule N greetings`.
5. В день события открыть карточку из `Home`, `Calendar` или профиля человека.
6. При необходимости отредактировать текст.
7. Нажать `Open Messages`, `Open Mail` или `Mark complete`.
8. Для SMS/email проверить содержимое в системном composer и самостоятельно нажать `Send`.

Touch Point считает действие завершённым, если системный composer сообщает результат `.sent`, либо если пользователь вручную завершает действие типа `Reminder only`. Это не подтверждение доставки или прочтения получателем.

## 3. Основные сущности

### Person — человек

Единая сущность для клиентов, коллег, друзей и семьи. У человека есть:

- имя;
- необязательная организация;
- тип отношений: `Client`, `Family`, `Friend` или `Colleague`;
- телефон и email;
- предпочтительный способ связи;
- предпочтительный язык;
- часовой пояс IANA, например `America/Los_Angeles`;
- список ежегодных важных дат.

Разделения на отдельные модели «клиент» и «личный контакт» нет. Это позволяет менять фокус приложения, не перемещая и не преобразуя данные.

### Important Date — ежегодная важная дата

Дата хранится как месяц и день без года. Она описывает повторяющийся повод человека, но сама по себе ещё не является запланированным действием.

Доступные персональные поводы:

- `Birthday` — день рождения;
- `Home anniversary` — годовщина дома;
- `Wedding anniversary` — годовщина свадьбы;
- `Client appreciation` — дата благодарности клиенту, которую редактор сейчас позволяет сохранить. При этом годовой планировщик пока не использует введённые месяц и день, а ставит этот повод на фиксированное 5 ноября; это отмеченное ниже несоответствие текущей реализации.

`Thanksgiving` и `Christmas` рассчитываются как общие календарные события и не добавляются в профиль человека.

### Greeting Event — запланированное касание

Это конкретное действие в календаре: определённый человек, повод, абсолютные дата и время, способ связи, статус, готовый текст, необязательная тема письма и ссылка на использованную версию шаблона.

Текст материализуется в момент планирования. Если позже изменить или удалить исходный шаблон, уже созданное событие сохранит прежний текст и тему письма.

### Greeting Template — шаблон

Повторно используемый текст с контекстом применения. Шаблон может подходить для нескольких поводов, типов отношений, каналов и языка. Он также содержит оформление, принадлежность к коллекции, признаки избранного и варианта по умолчанию, локальную историю версий и статистику использования.

Пустой список аудиторий, каналов или языков означает «подходит для любого значения».

### Template Group — коллекция шаблонов

Пользовательская папка с названием, иконкой, цветом и порядком отображения. Удаление коллекции не удаляет шаблоны: они переходят в `Ungrouped`.

## 4. Focus: что показывать в первую очередь

`Focus` — это фильтр и система приоритетов, а не тип аккаунта и не неизменная идентичность пользователя.

- `Work` фильтрует `Home` и `People` до клиентов и коллег, в планировщике начинает с `Client`, а рабочие поводы поднимает выше.
- `Personal` фильтрует `Home` и `People` до семьи и друзей, в планировщике начинает с `Family`, а личные поводы поднимает выше.
- `All` показывает на `Home` и в `People` все отношения и выбран по умолчанию на первом запуске.

Изменение Focus:

- сразу влияет на `Home` и список `People`;
- меняет начальные фильтры и порядок поводов в `Plan the Year`;
- не удаляет и не переносит людей, даты, шаблоны и планы;
- не ограничивает возможности: все типы отношений и поводов по-прежнему доступны;
- не фильтрует вкладку `Calendar`: там доступны события всех типов отношений с учётом выбранного фильтра статуса;
- не фильтрует библиотеку `Templates`, но определяет начальный повод при создании нового шаблона.

Focus и факт прохождения первого запуска сохраняются отдельно от основной базы в `UserDefaults`. Старое значение `professional` мигрирует в `work`, а `personal` — в `personal`.

## 5. Навигация приложения

После onboarding доступны четыре основные вкладки:

| Вкладка | Для чего нужна |
| --- | --- |
| `Home` | Понять ближайшее действие, оценить нагрузку и начать массовое планирование |
| `Calendar` | Увидеть все запланированные касания и отфильтровать их по статусу |
| `People` | Вести людей, контакты, предпочтения и ежегодные даты |
| `Templates` | Управлять библиотекой текстов, их контекстом, версиями и коллекциями |

`Settings` открывается по шестерёнке с `Home`. Детали события открываются из `Home`, `Calendar` и профиля человека.

## 6. Первый запуск — Onboarding

Экран предлагает выбрать начальный Focus и объясняет, что этот выбор можно безопасно поменять позже.

На экране находятся:

- название `Touch Point`;
- слоган `Plan once. Never forget again.`;
- карточки `Work`, `Personal`, `All`;
- выбранный по умолчанию вариант `All`;
- закреплённая кнопка `Continue`.

После `Continue` выбранный Focus сохраняется, onboarding больше не показывается. Разрешение на уведомления здесь намеренно не запрашивается: системный запрос появляется только после явного действия в настройках.

## 7. Home — центр ежедневной работы

`Home` отвечает на вопрос «что мне нужно сделать дальше?», а не повторяет маркетинговое описание продукта.

### Верхняя сводка

Показывает сегодняшнюю дату, число событий на ближайшие семь дней и число действий со статусом `Ready`. При рабочем или личном Focus в текст добавляется соответствующий контекст.

### Next action

Показывает первое незавершённое и непропущенное событие. Уже наступившее действие `Ready` получает приоритет над будущими `Planned`, затем события сортируются по дате.

Карточка содержит:

- человека и его инициалы;
- повод;
- статус;
- дату и время в часовом поясе получателя;
- `Prepare greeting` для готового действия или `View greeting` для будущего.

Если подходящих событий нет, секция остаётся без карточки.

### Plan greetings

Главный вход в массовое планирование. Он открывает четырёхшаговый сценарий `Plan the Year`.

### Upcoming

Показывает события в выбранном горизонте:

- `Today` — один календарный день;
- `7 days` — семь дней;
- `30 days` — тридцать дней.

Список учитывает текущий Focus. Строка показывает человека, повод, дату в часовом поясе получателя и способ связи. Пустой диапазон обозначается как `Nothing scheduled`.

## 8. People — люди и исходные даты

### Список людей

Вверху находится segmented picker Focus. Он меняет видимый набор людей и глобальную настройку Focus.

Поиск работает по:

- имени;
- email;
- организации.

При `All` люди сортируются по имени. В `Work` и `Personal` сначала учитывается приоритет типа отношений, затем имя.

### Создание и редактирование человека

Один и тот же редактор используется для добавления и изменения. Для сохранения нужны:

- непустое имя;
- хотя бы один контакт: телефон или email.

Формат email и телефона отдельно не проверяется. Дополнительно можно указать организацию, отношения, предпочтительный способ связи, язык и часовой пояс.

При `Work` новый человек с типом `Client` получает заголовки `New client` и `Add client`; структура данных от этого не меняется.

Доступные часовые пояса в picker:

- `America/Los_Angeles`;
- `America/Denver`;
- `America/Chicago`;
- `America/New_York`;
- `Europe/London`;
- `Europe/Paris`;
- `Asia/Tokyo`;
- `Australia/Sydney`.

Если в уже сохранённых данных есть другой идентификатор, редактор сохраняет и показывает его вместе со стандартным списком.

### Важные даты в редакторе

Новая дата по умолчанию получает сегодняшний месяц и день и повод `Birthday`. В редакторе выбираются повод, месяц и допустимый день месяца.

Добавление, изменение и удаление дат сначала происходят внутри черновика человека. Они попадают в постоянное хранилище только после основного `Add` или `Save`. Закрытие редактора через `Cancel` отбрасывает весь черновик.

### Профиль человека

Показывает:

- имя, организацию или тип отношений;
- доступные телефон и email;
- предпочтительный канал, язык и часовой пояс;
- важные даты;
- все запланированные касания этого человека со статусами.

Из списка событий можно перейти в `Greeting Detail`. Кнопка `Edit` открывает редактор человека. Удаление человека в текущей версии не реализовано.

## 9. Plan the Year — массовое планирование

Это основной сценарий Touch Point. Все выборы хранятся в черновике экрана и не изменяют календарь до финального подтверждения.

### Шаг 1: People

Нужно выбрать одного или нескольких людей. Доступен фильтр по типу отношений и кнопка `Select all` / `Clear`, которая действует только на текущий видимый список. Уже выбранные люди остаются выбранными при смене фильтра.

Начальный фильтр зависит от Focus:

- `Work` → `Client`;
- `Personal` → `Family`;
- `All` → все отношения.

Без выбранного человека продолжить нельзя.

### Шаг 2: Occasions

Можно выбрать один или несколько поводов:

- `Birthday`;
- `Home anniversary`;
- `Wedding anniversary`;
- `Thanksgiving`;
- `Christmas`;
- `Client appreciation`.

По умолчанию выбран `Birthday`. Порядок списка зависит от Focus, но набор всегда полный. Без выбранного повода продолжить нельзя.

### Шаг 3: Message

#### Способ связи

По умолчанию включено `Use each person's preferred method`. Тогда каждый человек получает собственный сохранённый способ связи.

Если выключить настройку, ко всем создаваемым событиям применяется один выбранный вариант:

- `Text message`;
- `Email`;
- `Reminder only`.

#### Шаблон на повод

Для каждого выбранного повода можно оставить `Automatic recommendation` или выбрать конкретный шаблон. Список не показывает архивные шаблоны и учитывает повод и, когда выбран единый канал, совместимость с ним.

#### Исключения для конкретных людей

`Customize specific people` позволяет назначить отдельный шаблон конкретному человеку для конкретного повода. Такой override нужен для редких случаев; для остальных получателей продолжает работать автоматический подбор.

Финальный приоритет выбора:

1. шаблон для конкретного человека и повода;
2. общий выбор для повода;
3. автоматическая рекомендация.

### Шаг 4: Review

Перед сохранением показываются:

- число выбранных людей;
- точное число новых событий, которые можно создать;
- единый способ связи или `Preferred methods`;
- выбранный подход к шаблонам;
- исключения для отдельных получателей;
- пример уже персонализированного текста.

Количество создаваемых событий может быть меньше произведения «люди × поводы»: человек может не иметь нужной даты, либо такое событие уже существует.

При нулевом результате кнопка планирования отключена. В остальных случаях `Schedule N greetings` создаёт события и показывает подтверждение с фактическим количеством.

### Как рассчитывается дата

Для каждого сочетания человека и повода ищется ближайшее наступление сегодня или в следующем календарном году по часовому поясу получателя.

- `Birthday`, `Home anniversary` и `Wedding anniversary` используют сохранённую персональную дату соответствующего типа.
- `Thanksgiving` — четвёртый четверг ноября.
- `Christmas` — 25 декабря.
- `Client appreciation` — 5 ноября для каждого выбранного человека; сохранённая одноимённая важная дата сейчас игнорируется планировщиком.
- Время действия — 09:00 в часовом поясе получателя.
- 29 февраля в невисокосном году превращается в последний допустимый день февраля.
- Планирование позже 09:00 в тот же локальный день получателя всё равно оставляет событие на сегодня: сравнение выполняется с началом дня.
- Дубликат того же человека и повода в тот же локальный день не создаётся независимо от его статуса.

Примечание о `Client appreciation`: редактор человека и `Occasion.contactSpecificCases` трактуют его как персональную дату, но `AppStore.occurrenceDate` всегда использует 5 ноября. Документ фиксирует это фактическое расхождение, а не скрывает его за предполагаемым продуктовым замыслом.

### Как автоматически выбирается шаблон

Сначала удаляются архивные и не относящиеся к поводу варианты. Затем проверяется совместимость с отношениями, каналом и языком. Пустое ограничение шаблона считается универсальным.

Подходящие шаблоны получают баллы:

| Признак | Баллы |
| --- | ---: |
| Явное совпадение типа отношений | 16 |
| Явное совпадение канала | 8 |
| Явное совпадение языка | 8 |
| `Default for this context` | 4 |
| `Favorite` | 2 |
| Встроенный шаблон | 1 |

При равенстве баллов выше ставится чаще использованный, затем более недавно обновлённый шаблон, затем UUID для стабильного результата.

### Что сохраняется при планировании

В событие записываются:

- рассчитанный абсолютный момент времени;
- итоговый способ связи;
- уже подставленный текст;
- уже подставленная тема email;
- идентификатор шаблона;
- номер использованной версии шаблона.

Также создаётся локальная запись статистики `scheduled`, увеличиваются `usageCount` и `lastUsedAt` шаблона.

## 10. Calendar — полный список событий

`Calendar` показывает все события независимо от Focus. Они сортируются по времени и группируются по календарному дню устройства. При этом дата в строке форматируется в часовом поясе получателя.

Фильтр в toolbar:

- `All`;
- `Planned`;
- `Ready`;
- `Completed`;
- `Skipped`.

Если ничего не найдено, экран предлагает сменить фильтр или запланировать новые события. Нажатие на строку открывает детали.

## 11. Статусы события

| Статус | Что означает |
| --- | --- |
| `Planned` | Действие запланировано на будущее |
| `Ready` | Дата `Planned` уже наступила; статус вычисляется динамически |
| `Completed` | Пользователь завершил системный compose-flow или вручную отметил reminder |
| `Skipped` | Пользователь решил пропустить действие; оно остаётся в истории и календаре |

`Ready` не записывается поверх исходного `Planned`: приложение вычисляет его при показе. Поэтому время автоматически переводит событие из визуального `Planned` в `Ready` без отдельной фоновой миграции.

`Completed` не означает `Delivered`, `Opened` или `Read`. Touch Point не располагает такой информацией.

Пропущенное событие можно восстановить. Оно возвращается в `Planned`, а если дата уже прошла, интерфейс сразу покажет вычисленный `Ready`.

## 12. Greeting Detail — подготовка конкретного касания

Экран показывает:

- человека, повод и статус;
- дату и время в часовом поясе получателя;
- способ действия;
- IANA-идентификатор часового пояса;
- эквивалент `Your time`, если часовой пояс устройства отличается;
- точный текст сообщения;
- исходный шаблон и его версию, если связь сохранилась.

Подпись о версии подчёркивает, что обновление шаблона не меняет уже запланированный текст.

### Редактирование текста

Кнопка `Edit` открывает отдельный редактор. `Save` заменяет текст только текущего события; usage-событие `messageEdited` записывается лишь тогда, когда текст действительно изменился. `Save as template` создаёт новый шаблон с поводом, отношениями, каналом и языком текущего события, но не заменяет исходный шаблон и не меняет событие автоматически.

Из этого же редактора можно вызвать Apple Intelligence.

### SMS

`Open Messages`:

1. проверяет наличие телефона;
2. проверяет доступность системного Messages composer;
3. записывает usage-событие `composerOpened`;
4. открывает `MFMessageComposeViewController` с получателем и текстом;
5. оставляет финальную кнопку `Send` пользователю;
6. при результате `.sent` переводит событие в `Completed`.

Отмена не меняет статус. Ошибка показывается локальным alert.

### Email

`Open Mail` работает аналогично, но передаёт в `MFMailComposeViewController` получателя, тему и текст. Если у события нет подготовленной темы, используется название повода.

Если на устройстве не настроен Mail или отсутствует email человека, приложение объясняет причину и не пытается использовать сторонний сервис.

### Reminder only

`Mark complete` сразу переводит событие в `Completed`, потому что отправлять через системный composer нечего.

### Skip и Restore

`Skip this greeting` требует подтверждения. Событие остаётся в `Calendar` со статусом `Skipped` и может быть возвращено кнопкой `Restore greeting`.

## 13. Templates — библиотека сообщений

### Зачем нужна библиотека

Шаблоны позволяют один раз определить подходящий тон для повода, аудитории, канала и языка, а затем автоматически получать персонализированный текст при планировании.

В стартовом состоянии есть 13 встроенных шаблонов в коллекциях `Personal`, `Work` и `Seasonal`.

### Области библиотеки

- `All templates` — все активные шаблоны;
- `Favorites` — отмеченные звездой;
- `Recently used` — имеющие `lastUsedAt`;
- `Ungrouped` — без коллекции;
- `Archived` — архивные;
- `Most used` — имеющие хотя бы одно плановое использование;
- `Missing default` — контексты, для которых нет активного шаблона по умолчанию;
- пользовательские коллекции.

### Поиск и фильтры

Текстовый поиск просматривает название, тело, тему email, поводы, аудитории, каналы и языки.

Независимые фильтры можно комбинировать:

- повод;
- аудитория/тип отношений;
- канал;
- язык;
- область/коллекция.

При фильтрации по аудитории, каналу или языку универсальные шаблоны с пустым ограничением также считаются подходящими.

### Сортировки

- `Recommended` — default, favorite, usage, свежесть обновления;
- `Name` — по названию;
- `Most used` — по счётчику и последнему использованию;
- `Recently updated` — по времени изменения.

В `Recently used` и `Most used` рекомендуемая сортировка дополнительно учитывает смысл выбранной области.

### Действия со строкой шаблона

- обычное нажатие — открыть редактор;
- leading swipe — добавить в избранное или убрать;
- trailing swipe — дублировать, архивировать встроенный, удалить пользовательский или восстановить архивный;
- context menu — редактирование, дубликат, избранное, локальное approval и lock.

Удалить можно только пользовательский незаблокированный шаблон, и действие требует подтверждения. Заблокированный шаблон сначала нужно разблокировать либо продублировать. Старые события сохраняют материализованный текст. Встроенный шаблон удалить нельзя, но можно архивировать.

## 14. Редактор шаблона

### Основные поля

- название;
- коллекция;
- иконка;
- цвет;
- один или несколько поводов;
- аудитории;
- каналы;
- один язык или универсальный вариант;
- необязательная тема email;
- тело сообщения.

Тема email показывается, если шаблон универсален по каналу или включает `Email`.

### Переменные

Поддерживаются только:

| Переменная | Подстановка |
| --- | --- |
| `{{first_name}}` | Первое слово из имени человека |
| `{{name}}` | Полное имя |
| `{{organization}}` | Организация, в том числе пустая строка |
| `{{occasion}}` | Отображаемое название повода |
| `{{date}}` | Длинная локализованная дата события |

Кнопки переменных добавляют токен в конец тела сообщения. Подстановка происходит при preview и при создании события.

Валидация запрещает сохранение, если:

- нет названия;
- нет тела сообщения;
- не выбран ни один повод;
- фигурные скобки токена не сбалансированы;
- встречается неподдерживаемый токен.

### Preview

Показывает итоговый текст и тему с подстановками. Для примера выбирается первый подходящий человек из локального store, а если его нет — условный `Alex Morgan` из `Northstar`.

### Favorite и Default

`Favorite` влияет на подбор и рекомендуемую сортировку.

`Default for this context` даёт дополнительные баллы при подборе. При назначении default приложение снимает этот признак с других шаблонов, у которых пересекается повод и полностью совпадают наборы аудиторий, каналов и языков.

### Approval и Lock

Это **локальные организационные метки одного устройства**, а не командное согласование, роли или защита данных.

- `Approved` отмечает локально проверенный текст.
- `Locked` запрещает изменение содержимого.
- встроенные шаблоны всегда approved и locked;
- встроенный шаблон нельзя разблокировать или лишить approval;
- для настройки встроенного или любого locked-шаблона предлагается `Duplicate to edit`;
- изменение содержимого approved-шаблона требует подтверждения и снимает approval.

Изменения избранного, default, архива, approval, lock и статистики не создают контентную версию.

### История версий

При изменении содержимого пользовательского незаблокированного шаблона:

1. предыдущее содержимое сохраняется как `TemplateRevision`;
2. номер текущей версии увеличивается;
3. approval снимается;
4. `updatedAt` обновляется.

Хранится не более 50 прежних версий на шаблон. Восстановление старой версии аддитивно: прежнее текущее содержимое сначала попадает в историю, а выбранный snapshot становится новой текущей версией. История не переписывается задним числом.

### Статистика использования

В редакторе показываются:

- `Lifetime schedules` — накопленный `usageCount`;
- `Last used` — последнее плановое использование;
- `Tracked schedules` — число локальных записей внутреннего типа `scheduled`;
- `Composer opened` — `composerOpened`;
- `Completed` — `completed`;
- `Skipped` — `skipped`;
- `Message edited` — `messageEdited`.

Это журнал действий внутри Touch Point, а не аналитика доставки или реакции получателя. Запись не содержит текст сообщения или идентификатор человека, но может содержать UUID события, повод, канал и версию шаблона.

## 15. Коллекции шаблонов

В `Manage collections` можно:

- создать коллекцию;
- изменить название, иконку и цвет;
- менять порядок drag-and-drop;
- удалить коллекцию.

Название обязательно. Удаление не затрагивает шаблоны: их `groupID` очищается, и они становятся `Ungrouped`.

## 16. Импорт и экспорт библиотеки

`Export library` создаёт JSON-файл `TouchPoint Templates`, содержащий версию payload, шаблоны и коллекции.

`Import library`:

- читает JSON;
- игнорирует архивные шаблоны и коллекции;
- создаёт новые UUID, поэтому не обновляет существующие записи по ID;
- сопоставляет новые ID коллекций с импортированными шаблонами;
- добавляет содержимое к текущей библиотеке;
- не выполняет дедупликацию по названию или тексту;
- не импортирует локальную историю версий и usage как отдельные разделы хранилища;
- сбрасывает у новых шаблонов номер версии до 1, usage, `lastUsedAt`, built-in, approval и lock, потому что создаёт новые пользовательские объекты через ограниченный initializer;
- не трактует approval/lock как переносимые командные права.

Ошибки чтения и записи показываются alert `Template library`.

## 17. Apple Intelligence

Генератор доступен в редакторе шаблона и редакторе конкретного сообщения.

Пользователь выбирает тон:

- `Warm`;
- `Professional`;
- `Friendly`;
- `Heartfelt`;
- `Brief`.

Можно добавить необязательные указания. Экран показывает используемый контекст: повод, аудиторию, канал и язык. Результат доступен для редактирования до нажатия `Use`.

Генерация работает на устройстве через Apple Foundation Models и требует:

- iOS 26 или новее;
- совместимое устройство;
- включённый Apple Intelligence;
- готовую загруженную модель;
- поддерживаемую текущую локаль.

На iOS 18–25 и при любой недоступности модели ручное редактирование продолжает работать. Экран объясняет конкретную причину.

При генерации шаблона модели передаётся инструкция вставить буквальный `{{first_name}}` ровно один раз, но приложение отдельно не валидирует выполнение этого требования после ответа. При генерации сообщения для конкретного события используется имя получателя. Инструкция также запрещает выдумывать личные факты и просит вернуть только готовый текст без анализа.

## 18. Settings и локальные напоминания

### Focus

В настройках можно в любой момент выбрать `Work`, `Personal` или `All`. Изменение применяется сразу и не меняет сами данные.

### Разрешение на уведомления

Состояния:

- `Checking` — статус читается;
- `Not enabled` — запрос ещё не показывался;
- `Enabled` — разрешение выдано;
- `Off` — пользователь запретил уведомления.

`Enable reminders` явно вызывает системный запрос. При `Off` кнопка `Open System Settings` ведёт в настройки iOS.

### Как синхронизируется очередь

После запуска, изменения людей или событий и при возвращении приложения в active state Touch Point:

1. находит свои pending notifications по префиксу `touchpoint.greeting.`;
2. удаляет их;
3. заново создаёт уведомления для будущих событий, которые не `Completed` и не `Skipped`.

Уведомление создаётся на сохранённый абсолютный момент времени. В `userInfo` записывается только UUID события — без телефона, email и текста сообщения.

Текст уведомления говорит, что пора открыть Touch Point и продолжить действие. Он не обещает автоматическую отправку.

## 19. Даты и часовые пояса

Touch Point разделяет два вида времени:

- ежегодный повод — месяц и день без года;
- запланированное действие — абсолютный `Date`, рассчитанный на 09:00 в часовом поясе получателя.

Это предотвращает случайный сдвиг дня рождения при переводе полуночи между часовыми поясами.

В деталях события всегда показываются дата, время и зона получателя. Если зона устройства другая, показывается `Your time`. На компактных строках дата также форматируется в зоне получателя, хотя группировка `Calendar` выполняется по локальному дню устройства.

При неизвестном или некорректном IANA-идентификаторе расчёт и форматирование переходят на текущий часовой пояс устройства.

## 20. Ручная отправка и границы обещаний

Touch Point не подключён к SMS/email-провайдеру и не отправляет ничего в фоне.

Он умеет только:

- подготовить адресата, тему и текст;
- открыть системный Messages или Mail composer;
- получить локальный результат compose-flow;
- записать внутренний статус действия.

Он не умеет достоверно показывать:

- доставку;
- открытие;
- прочтение;
- клик;
- отказ от рассылки;
- ошибки провайдера;
- аналитику получателя.

Поэтому формулировка `Completed` относится к действию пользователя, а не к состоянию сообщения в сети оператора или почтового сервиса.

## 21. Локальное хранение

Приложение не использует backend, CloudKit, Core Data, SwiftData или SQLite. Основное состояние хранится одним versioned JSON snapshot:

`Application Support/TouchPoint/touchpoint-data.json`

Текущая версия схемы — `v5`. Snapshot содержит:

- людей и их контакты;
- ежегодные даты;
- события и их материализованные тексты;
- шаблоны и коллекции;
- историю версий шаблонов;
- metadata-only usage.

Особенности:

- даты кодируются в ISO 8601;
- enum сохраняются стабильными машинными значениями, а не видимыми английскими подписями;
- запись выполняется атомарно;
- каждый mutation-метод сохраняет полный snapshot;
- читаются версии `v1`–`v5`;
- совместимость старых данных обеспечивается tolerant decoding и обновлением библиотеки до текущих групп и built-in-метаданных.

Если файла ещё нет, приложение записывает демонстрационный workspace с пятью людьми, шестью событиями и встроенной библиотекой. То есть первый production-запуск текущего прототипа начинается не с пустой базы.

Если snapshot повреждён или имеет неподдерживаемую версию, приложение показывает демонстрационные данные, но без рабочего URL сохранения. Ошибка выводится inline в `People` и `Templates`, а изменения этой сессии не сохраняются.

Отдельного слоя шифрования JSON в приложении нет. За защиту файла отвечают системные механизмы контейнера приложения.

## 22. Стартовые данные и встроенная библиотека

Демонстрационное состояние содержит:

- Anna Smith — Client;
- Marcus Lee — Friend;
- Sofia Ramirez — Client, предпочитает email и Spanish;
- Daniel Kim — Colleague;
- Maya Thompson — Family.

Также создаются коллекции `Personal`, `Work`, `Seasonal` и 13 встроенных англо- и испаноязычных шаблонов для дней рождения, годовщин, благодарности, Thanksgiving и Christmas.

Встроенные шаблоны неизменяемы, локально approved и locked. Их можно архивировать или дублировать для настройки.

## 23. Архитектура реализации

Приложение написано на SwiftUI. Минимальная версия — iOS 18, целевые устройства — iPhone и iPad, Swift language mode — 5. Внешних Swift Package Manager-зависимостей нет.

`ContentView` создаёт единый `AppStore.live` и `AppPreferences`, хранит их как observable state и передаёт через SwiftUI Environment.

`AppStore` сейчас совмещает несколько ролей:

- observable state container;
- локальное repository;
- сервис сохранения;
- планировщик дат;
- resolver и renderer шаблонов;
- журнал локального usage.

Такое устройство удобно для прототипа, но при росте приложения потребует разделения ответственности.

### Карта ключевых файлов

| Файл | Ответственность |
| --- | --- |
| `TouchPoint/TouchPointApp.swift` | Точка входа приложения |
| `TouchPoint/ContentView.swift` | Onboarding/main shell, Environment, пересборка уведомлений |
| `TouchPoint/Models/AppModels.swift` | Доменные модели, стабильные enum ID, validation и Codable-совместимость |
| `TouchPoint/Data/AppStore.swift` | Состояние, бизнес-правила, планирование, шаблоны и JSON persistence |
| `TouchPoint/Preferences/AppPreferences.swift` | Focus и onboarding в `UserDefaults` |
| `TouchPoint/Views/Onboarding/OnboardingView.swift` | Первый запуск |
| `TouchPoint/Views/Home/HomeView.swift` | Сводка, next action, upcoming и вход в planning |
| `TouchPoint/Views/People/PeopleView.swift` | Список, профиль, редактор людей и важных дат |
| `TouchPoint/Views/PlanYear/PlanYearView.swift` | Четырёхшаговый планировщик |
| `TouchPoint/Views/Calendar/RelationshipCalendarView.swift` | Все события и фильтр статусов |
| `TouchPoint/Views/Greetings/GreetingDetailView.swift` | Детали, редактирование, handoff, skip/restore |
| `TouchPoint/Views/Templates/TemplatesView.swift` | Библиотека, редактор, коллекции, import/export, history и governance |
| `TouchPoint/Platform/SystemComposer.swift` | UIKit-обёртки Messages и Mail |
| `TouchPoint/Platform/LocalNotificationScheduler.swift` | Разрешение и reconciliation локальных уведомлений |
| `TouchPoint/Platform/AppleGreetingGenerator.swift` | On-device генерация через Foundation Models |
| `TouchPoint/DesignSystem/TouchPointDesign.swift` | Цвета, метрики и общие SwiftUI-компоненты |
| `docs/design-language.md` | Визуальный и продуктовый контракт |

## 24. Визуальный язык и доступность

Интерфейс использует нативные navigation stacks, tab bar, lists, sheets, menus, forms, system composers и grouped backgrounds.

Основные reusable-компоненты:

- `SurfaceCard` — объединяет связанные данные или действия;
- `IconTile` — компактная семантическая иконка;
- `PersonAvatar` — инициалы с цветом типа отношений;
- `SectionHeading` — заголовок секции с необязательным действием;
- `StatusPill` — текстовый статус, не полагающийся только на цвет;
- `TouchPointPrimaryButtonStyle` — одна доминирующая кнопка на поверхность или шаг.

Смысловые цвета:

- accent/indigo — навигация и обычные действия;
- coral — день рождения;
- rose — годовщина отношений;
- amber — сезонный повод и `Ready`;
- forest — Christmas и `Completed`;
- teal — благодарность и профессиональное касание.

Иконки не должны быть единственным носителем смысла. Ключевые состояния сопровождаются текстом, icon-only controls имеют accessibility labels, используются системные Dynamic Type, dark mode и locale-aware date formatting.

## 25. Приватность и безопасность текущей версии

- Контакты, планы и тексты остаются в локальном контейнере приложения.
- Сетевого API, аккаунта, аналитического SDK и облачной синхронизации нет.
- Apple Intelligence работает on-device.
- В notification payload нет контактных данных и текста.
- Usage — локальные технические события, а не поведение получателя.
- Approval и lock не являются security boundary.
- Отдельного прикладного шифрования JSON и Keychain-слоя нет.

## 26. Текущие ограничения

- Интерфейс прототипа только на английском; строковых localization resources пока нет.
- Нет удаления людей.
- Нет аккаунтов, командных пространств, ролей и синхронизации между устройствами.
- Нет импорта контактов из системной адресной книги.
- Нет автоматического переноса пропущенного события на следующий год.
- Нет фоновой отправки и delivery/open analytics.
- Нет unit-, integration- и UI-тестов или отдельного test target.
- Любое изменение сохраняет весь JSON snapshot, а очередь уведомлений полностью пересобирается.
- Первый запуск использует sample workspace, а не пустое состояние.
- Формат телефона и email не валидируется.
- `AppStore` объединяет хранение, бизнес-логику и прикладные сервисы.
- Документ `docs/design-language.md` местами всё ещё упоминает схему `v2`, хотя текущая реализация использует `v5`.

## 27. Открытые продуктовые вопросы

- Должен ли первый релиз одинаково подчёркивать Messages и Mail или начинать прежде всего с Messages?
- Следует ли автоматически переносить пропущенные события на следующий год?
- Какие типы событий должны хранить год, а не только месяц и день?
- Нужны ли одному человеку несколько ролей, домохозяйств или организаций?
- Появятся ли общий workspace, серверная синхронизация и правила разрешения конфликтов?
- Должен ли `Client appreciation` оставаться персональной датой или стать общей датой 5 ноября?
- Должен ли production-onboarding начинаться с пустой базы вместо sample workspace?

## 28. Чек-лист обновления этого файла

Перед завершением любой задачи, меняющей поведение Touch Point, проверить:

- добавился или изменился экран — обновлены его цель, элементы, состояния и переходы;
- изменился пользовательский сценарий — обновлены шаги и условия доступности действий;
- изменилось бизнес-правило — описаны приоритеты, fallback и крайние случаи;
- изменилась модель или persistence version — обновлены сущности, путь, миграция и совместимость;
- изменилась интеграция — описаны разрешения, ошибки, приватность и границы обещаний;
- изменился статус, аналитическое событие или термин — обновлена его точная семантика;
- добавилось ограничение или решён открытый вопрос — исправлены соответствующие разделы;
- документация остаётся на русском языке и соответствует фактическому коду.

## 29. История документа

- **2026-09-03 — первоначальная итерация.** Описаны все реализованные экраны, основные и вложенные сценарии, модели, алгоритм планирования, выбор и версионирование шаблонов, системные composers, Apple Intelligence, уведомления, локальное хранение, приватность, ограничения и открытые вопросы.
